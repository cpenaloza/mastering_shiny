# TODO add auto load new packages. 

install.packages(c(
  "gapminder", "ggforce", "globals", "openintro", "reactable", "RSQLite", "shiny", 
  "shinycssloaders", "shinyFeedback", "shinythemes", "testthat", 
  "thematic", "tidyverse", "vroom", "waiter", "xml2", "zeallot" 
))
